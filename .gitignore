# macOS
**/.DS_Store

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini

# Python — compiled + packaging
__pycache__/
**/__pycache__/
*.pyc
*.py[cod]
.Python
*.egg-info/
dist/
build/

# Python — virtual envs
.venv/
venv/
env/
ENV/

# Python — coverage
.coverage
.coverage.*
htmlcov/
coverage.xml

# Secrets — API keys and credentials are the #1 leak vector for HA integrations
.env
.env.*
!.env.example
secrets.yaml
.secrets
*.secret
credentials.json
token.json
auth.json
*.pem
*.key
*.p12
*.pfx
local_*.py
private_*.py

# Tool caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# IDEs / editors
.vscode/
.idea/
*.iml
*.swp
*.swo
*~

# Claude / AI tooling — transcripts, plans, local state
.claude/
.claude-cache/
.claude-*
CLAUDE.local.md
*.local.md
.aider*
.cursor/
.cursorrules.local
.continue/
.windsurf/

# HA-local test state (from running a local HA against the integration)
config/
home-assistant.log*
home-assistant_*.log*
.storage/

# Logs / pip
*.log
pip-log.txt
pip-delete-this-directory.txt

# Python — version pin
.python-version

# Build
target/

# Local test data (HA exports, analysis scratch)
test/

# Local dev tooling (push scripts, etc. — not shared)
scripts/

# Card build — source-of-truth is src/; bundle in www/ is tracked.
node_modules/
*.js.map

# Local reference copies during in-flight refactors
*.bak

# ---------------------------------------------------------------------------
# Fallow code-quality tool — config + caches. Personal tooling; ship the
# repo without forcing fallow on contributors. Drop the rule if you decide
# fallow should be a project-wide standard.
# ---------------------------------------------------------------------------
.fallowrc.json
.fallowrc.jsonc
fallow.toml
.fallow/
